const a=7;

console.log(a);


//-----------------

let string;

string = prompt("문자열을 입력하세요");
 
console.log(string);

/*
 
1.
상수
변하지 않는 고정된 값

상수 만드는 방법
const 상수이름 = 데이터; 
* 선언과 초기화가 동시에 이루어져야 함.
* 상수형 변수에 한번 값을 대입하면 다른 값으로 변경할 수 없다, 대입
  할 경우 오류가 난다.

2.
window.prompt();
- 사용자로부터 문자열을 입력받을 수 있는 다이얼로그 박스를 열어주는
메소드, 괄호 안에 안내 텍스트를 입력할 수 있다(사진 참고).
- 사용자로부터 입력받은 문자열을 반환한다(자신이 저장된 변수에).

3.
constant : 1. 상수. 2. 지속적인, 끊임없는, 거듭되는, 변함없는 

*/
